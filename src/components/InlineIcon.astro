---
import { readFileSync } from "node:fs";
import { resolve } from "node:path";

interface Props {
  name: string;
}

const { name } = Astro.props;
const svg = readFileSync(resolve(process.cwd(), "src/icons", `${name}.svg`), "utf8");
---

<Fragment set:html={svg} />
